{% extends 'base.html.twig' %}

{% block title %}Renouvellement du mot de passe{% endblock %}

{% block body %}


<div class="container ">
    <div class="row jumbotron bg-light mt-5">
        <div class="col-md-12">
            <h1 class="my-5">Renouvellement du mot de passe</h1>
            {{form_start(form)}}
            {{form_row(form.pswd.first)}}
            {{form_row(form.pswd.second)}}
            <button class="btn btn-primary" type="submit">Envoyer</button>
            {{form_end(form)}}
        </div>
    </div>
</div>

{% endblock %}